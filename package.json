{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "project for demo",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/Demo.postman_collection.json -e tests/Automation.postman_environment.json -d tests/test.txt --reporters json --reporter-json-export  newman.json --delay-request 5000"
  },
  "author": "supraja bandi",
 
  "dependencies": {
    "newman": "^3.9.3"
  }
}
